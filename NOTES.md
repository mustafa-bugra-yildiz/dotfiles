## Notes

- Ghostty is broken on nixpkgs, using Homebrew casks for it for now.
- Formatting `sh` files with `shfmt` in `setup.sh` does not work.

## Resources

Outdated, but helpful: https://davi.sh/til/nix/nix-macos-setup/
